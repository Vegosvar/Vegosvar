        <div class="col-sm-8 col-md-4 sidebar">
          <h2>Signatur</h2>
          <div class="card profile">
            <div class="row">
              <div class="col-xs-4 col-sm-3 col-md-4 avatar">
                <a href="/installningar"><img id="user-avatar" alt="" src="<% include functions/getUserAvatar %>"></a>
              </div>

              <div class="col-xs-8 col-sm-9 col-md-8 info">
                <a href="<%= user.info.website %>" target="_blank" title="<%= user.info.website %>"><h3><%= user.name.display_name %></h3></a>
  			         <p>
                  <span class="description"><% if(typeof user.info !== 'undefined') { %>
                    <%= typeof user.info.description !== 'undefined' ? user.info.description: 'Beskrivning saknas' %>
                  <% } else { %>
                    Beskrivning saknas
                  <% } %>
                </p>
              </div>
            </div>
          </div>
          <div class="card">
            <small><p style="margin: 0;"><strong>Tips</strong> Om du önskar vara anonym kan du välja det när du <a href="/ny">skapar en sida</a>. Din hemliga identitet är säker hos oss Batman.</p>
            </small>
          </div>
        </div>